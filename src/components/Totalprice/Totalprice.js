import React from 'react';

const Totalprice = (props) => {
    return (
        <div>
            <h2>Total price of Cosmetics: ${props.price}</h2>
        </div>
    );
};

export default Totalprice;